# Wiring notes

The example is simulator-only. A physical deployment would map the two logical motor paths to a selected PWM driver, document GPIO pins, verify voltage/current limits, and keep the 250 ms motion watchdog enforced by the runtime or MCU bridge.
