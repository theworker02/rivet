<p align="center"><img src="../../site/assets/rivet-logo.svg" alt="Rivet robotics infrastructure" width="520"></p>

# Benchmark tooling

Benchmark definitions live in `src/rivet/benchmarks.py` and feed evidence into `SkillGraph`. Future benchmark runners should write reproducible JSON results and never silently upgrade a robot to `authorized`.
