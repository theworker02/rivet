<p align="center"><img src="../../site/assets/rivet-logo.svg" alt="Rivet robotics infrastructure" width="520"></p>

# Diagnostic tooling

Use `python -m rivet doctor --verbose` for the supported diagnostic entry point. Additional Pi probes should be added here only when they have a simulator or safe-unavailable fallback.
